package org.usfirst.frc.team610.robot.commands;

import org.usfirst.frc.team610.robot.constants.ElectricalConstants;
import org.usfirst.frc.team610.robot.subsystems.DriveTrain;

import edu.wpi.first.wpilibj.command.Command;

/**
 *
 */
public class A_Drive extends Command {
	
	DriveTrain driveTrain;
	double tDistance;
	double cap;
	double error;
	double lastError;
	double tAngle = 0;

    public A_Drive(double tDistance, double cap) {
    	
    	driveTrain = DriveTrain.getInstance();
    	
        // Use requires() here to declare subsystem dependencies
        // eg. requires(chassis);
    }

    // Called just before this Command runs the first time
    protected void initialize() {
    	
    	
    }

    // Called repeatedly when this Command is scheduled to run
    protected void execute() {
    	double leftSpeed, rightSpeed, difference, avgDistance;
    	double p = ElectricalConstants.GYRO_P;
    	double d = ElectricalConstants.GYRO_D;
    	double currentAngle = driveTrain.getYaw();
    	while (currentAngle < -180) {
			currentAngle += 360;
		}
		while (currentAngle > 180) {
			currentAngle -= 360;
		}
		
		error = tAngle - currentAngle;
    	
		difference = error - lastError;
		
		leftSpeed = Math.min(cap, (tDistance - driveTrain.getAvgDistance()) * ElectricalConstants.ENCODER_P);
		rightSpeed = Math.min(cap, (tDistance - driveTrain.getAvgDistance()) * ElectricalConstants.ENCODER_P);
    	
		leftSpeed += error * p + difference * d;
    	rightSpeed -= error * p + difference * d;
		
		lastError = error;
    }

    // Make this return true when this Command no longer needs to run execute()
    protected boolean isFinished() {
        return false;
    }

    // Called once after isFinished returns true
    protected void end() {
    }

    // Called when another command which requires one or more of the same
    // subsystems is scheduled to run
    protected void interrupted() {
    }
}
